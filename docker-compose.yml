services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: devshowcase
      POSTGRES_USER: devshowcase
      POSTGRES_PASSWORD: devshowcase
    ports:
      - "5432:5432"
    volumes:
      - devshowcase_pgdata:/var/lib/postgresql/data

volumes:
  devshowcase_pgdata:
